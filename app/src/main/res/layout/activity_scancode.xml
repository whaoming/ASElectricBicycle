<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
  >
<RelativeLayout 
    android:layout_weight="6"
     android:layout_width="match_parent"
    android:layout_height="0dp"
    >
    <EditText 
        android:id="@+id/et_carid"
        android:layout_above="@+id/btn_scan"
         android:layout_width="match_parent"
         android:hint="模拟扫码获得的车辆id"
    android:layout_height="wrap_content"
        />
    <Button 
        android:layout_centerInParent="true"
        android:id="@+id/btn_scan"
         android:text="扫码"
        android:layout_width="wrap_content"
    android:layout_height="wrap_content"
        />
</RelativeLayout>

<!--  <Button 
    android:id="@+id/btn_unregister"
    android:layout_weight="1"
     android:layout_width="match_parent"
    android:layout_height="0dp"
    android:text="模拟扫描一辆未被注册过的车"
    />

<Button 
    android:id="@+id/btn_register"
    android:layout_weight="1"
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:text="模拟扫描一辆已经被注册过的车"
    />-->
     <Button
        android:id="@+id/btn_debug" 
        android:text="进入地图调试模式"
         android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        />

<Button 
    android:id="@+id/btn_login"
    android:layout_weight="1"
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:text="已有帐号?直接登陆"
    />
</LinearLayout>
